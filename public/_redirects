# Serve static assets directly - must come BEFORE the catch-all
/assets/*  /assets/:splat  200
/*.png  /:splat  200
/*.json  /:splat  200
/*.html  /:splat  200
/*.js  /:splat  200
/*.css  /:splat  200

# SPA fallback for all other routes (this must be LAST)
/*  /index.html  200
